(function() {
  var ERRORS;

  ERRORS = {
    MISSING_DOCUMENT_PARAM: function(trace) {
      return JSON.stringify({
        code: 'MDP1',
        message: 'You must specify a document as a string for parsing.',
        trace: trace
      });
    }
  };

  module.exports = ERRORS;

}).call(this);
